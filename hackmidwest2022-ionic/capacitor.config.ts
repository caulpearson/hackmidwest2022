import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.hackmidwest2022',
  appName: 'hackmidwest2022-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
